


let trainer = { name: 'Nag', sub: ".js" };

// let dynamicString = "the trainer " + trainer.name + " , killing with " + trainer.sub + " on us";


// string interpolation

// let dynamicString = `the trainer ${trainer.name.toUpperCase()} , teaching ${trainer.sub} to us`;


//-----------------------------------

let htmlTemplate = `

    <div>
        <h1>${trainer.name.toUpperCase()}</h1>
    </div>

`;